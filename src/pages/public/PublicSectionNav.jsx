/** Slim section bar matching enrollment step-header rhythm (HTML reference). */
export default function PublicSectionNav({ label }) {
  return (
    <div className="pub-section-nav">
      <div className="pub-section-nav-label">{label}</div>
    </div>
  )
}
